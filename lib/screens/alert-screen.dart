import 'package:flutter/material.dart';

class AlerScreen extends StatelessWidget {
   
  const AlerScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('AlerScreen'),
      ),
    );
  }
}